# Repository Cleanup Ledger

Date: 2026-07-02
Status: Active repository cleanup ledger

Repository cleanup means source-tree hygiene. This ledger records deletion or
untracking actions after each candidate is classified and checked against
current product authority, source, tests, docs, and fresh Graphify evidence.

Graphify evidence used for the first entries was `graphify-out/GRAPH_REPORT.md`
built from commit `071bd9d7`, matching `HEAD` on 2026-07-02.
Slice 3 refreshed Graphify before and after the legacy purge; the post-cleanup
code graph reported 9,997 nodes, 23,061 edges, and 678 communities.

## Deletion Ledger

| Path | Classification | Action | Reason | Replacement authority | Verification |
|---|---|---|---|---|---|
| `handoff-hyperplane.md` | historical reference | delete | root handoff duplicated old Hyperplane experiment notes; Hyperplane is now quarantined roadmap/reference, not active validation authority | `AGENTS.md`, `docs/architecture/hyperplane-evidence-loop.md` | `rg -n "handoff-hyperplane|Hyperplane Eval Experiment" AGENTS.md README.md CODE_MAP.md docs src tests` |
| `finalize-promote-dogfood.txt` | stale artifact | delete | one-line root scratch note with no active code, test, or doc consumer | `docs/control-room-mvp.md`, `docs/operator-centered-mission.md` | `rg -n "finalize-promote-dogfood|finalize promote ux works" AGENTS.md README.md CODE_MAP.md docs src tests` |
| `results/input_space_state_rule_0_plane_*.json` | generated/local runtime state | delete and ignore `/results/` | old root Hyperplane output; active Hyperplane artifacts are task-scoped under `.devflow/tasks/<task_id>/hyperplane-runs/<run_id>/` | `docs/architecture/hyperplane-evidence-loop.md`, `src/devflow/control_room/hyperplane_harness.py` | `rg -n "input_space_state_rule_0_plane|results/input_space" AGENTS.md README.md CODE_MAP.md docs src tests` |
| `public/` | compatibility bridge | delete | obsolete static marketing/simulator UI; current browser product is served from Python-bundled operating-layer assets | `AGENTS.md`, `docs/control-room-mvp.md`, `src/devflow/control_room/operating_layer_assets.py` | `rg -n "public/(index\\.html|app\\.js|styles\\.css)" AGENTS.md README.md CODE_MAP.md docs src tests`; `env PYTHONPATH=src:. .venv/bin/python -m pytest tests/test_skill_injection.py tests/test_operating_layer.py::test_operating_layer_assets_facade_keeps_split_asset_contract tests/test_operating_layer.py::test_operating_layer_server_serves_app_and_snapshot tests/test_operating_layer_lifecycle.py -q`; served `/healthz` + `/api/snapshot` + cache-busted UI body check |
| `tests/test_marketing_assets.py` | stale artifact | delete | test pinned the deleted static marketing/simulator files instead of active operating-layer assets | `tests/test_operating_layer.py`, `tests/test_operator_ui_browser.py` | `env PYTHONPATH=src:. .venv/bin/python -m pytest tests/test_skill_injection.py tests/test_operating_layer.py::test_operating_layer_assets_facade_keeps_split_asset_contract tests/test_operating_layer.py::test_operating_layer_server_serves_app_and_snapshot tests/test_operating_layer_lifecycle.py -q` |
| `src/devflow/_legacy/` | compatibility bridge | delete | pre-control-room runtime was no longer active authority and kept old software-factory imports alive | `AGENTS.md`, `docs/control-room-mvp.md`, `src/devflow/control_room/` | `rg -n "devflow\\._legacy|src/devflow/_legacy" src tests AGENTS.md README.md CODE_MAP.md docs`; `env PYTHONPATH=src:. .venv/bin/python -m pytest tests/test_architecture_boundaries.py -q` |
| `src/devflow/agents/` and pure top-level legacy shims | compatibility bridge | delete | files only re-exported deleted `_legacy` modules and forced old tests to remain valid | `AGENTS.md`, `docs/control-room-mvp.md`, `src/devflow/control_room/` | `rg -n "devflow\\.(agents|artifacts|context|repo_map|runner|orchestrator|model_gateway)|Legacy shim.*devflow\\._legacy" src tests AGENTS.md README.md CODE_MAP.md docs`; `env PYTHONPATH=src:. .venv/bin/python -m pytest tests/test_architecture_boundaries.py -q` |
| `src/devflow/schemas/` | compatibility bridge | delete | pre-control-room JSON schemas had no runtime/test consumer after the legacy purge and were only packaged as stale compatibility data | `AGENTS.md`, `docs/control-room-mvp.md`, `src/devflow/control_room/` | `rg -n "src/devflow/schemas|devflow/schemas|schemas/\\*\\.json|artifact\\.schema|dag\\.schema|diff_result\\.schema|failure_result\\.schema|repair_result\\.schema|review_result\\.schema" pyproject.toml README.md CODE_MAP.md docs src tests --glob '!docs/architecture/repository-cleanup-ledger.md' --glob '!docs/architecture/repository-cleanup-plan-2026-07-02.md' --glob '!docs/agent-handoff.md' --glob '!docs/legacy-quarantine.md' --glob '!docs/superpowers/**' --glob '!docs/archive/**'`; `env PYTHONPATH=src:. .venv/bin/python -m pytest tests/test_architecture_boundaries.py -q` |
| `docs/handoffs/` | historical reference | delete | milestone handoff files were stale resume context and current docs now point to active authority plus the cleanup ledger instead of per-milestone handoff archives | `AGENTS.md`, `docs/control-room-mvp.md`, `docs/operator-centered-mission.md`, `docs/architecture/repository-cleanup-ledger.md` | `rg -n "docs/handoffs|handoffs/" AGENTS.md README.md CODE_MAP.md docs src tests --glob '!docs/architecture/repository-cleanup-ledger.md' --glob '!docs/architecture/repository-cleanup-plan-2026-07-02.md' --glob '!docs/agent-handoff.md' --glob '!docs/superpowers/**' --glob '!docs/archive/**'` |
| `.devflow/` tracked materialization | generated/local runtime state | untrack root materialization | seed content is runtime-authoritative from `src/devflow/control_room/seed.py` and remains ignored local state | `src/devflow/control_room/seed.py`, `tests/test_devflow_init_structure.py`, `README.md` | `git ls-files .devflow`; `env PYTHONPATH=src:. .venv/bin/python -m pytest tests/test_devflow_init_structure.py -q` |
| `.agent/skills/` | compatibility/tooling bridge | delete duplicate tree | duplicated `skills/`, lacked `skills/hermes/` and `skills/token-optimization/`, and had a stale architecture rehab script copy | `skills/`, `.agent/workflows/`, `.agent/rules/`, `.github/skills/devflow/SKILL.md` | `diff -qr .agent/skills skills`; `rg -n "\\.agent/skills" .github .agent docs README.md AGENTS.md src tests --glob '!docs/archive/**' --glob '!docs/superpowers/**'`; `env PYTHONPATH=src:. .venv/bin/python -m pytest tests/test_devmode_git_bridge.py tests/test_architecture_rehab_skill_scripts.py tests/test_supervisor_operating_surface.py::test_hermes_docs_and_skill_flag_quarantined_checkout_path tests/test_supervisor_operating_surface.py::test_hermes_imessage_and_parallelism_docs_define_safe_boundaries -q` |

## Tombstone Index

| Deleted path pattern | Date | Reason | Replacement authority |
|---|---|---|---|
| `handoff-hyperplane.md` | 2026-07-02 | stale historical Hyperplane handoff; no longer current authority | `AGENTS.md`, `docs/architecture/hyperplane-evidence-loop.md` |
| `public/` | 2026-07-02 | obsolete static marketing/simulator UI; active browser UI is Python-bundled operating layer | `AGENTS.md`, `docs/control-room-mvp.md`, `src/devflow/control_room/operating_layer_assets.py` |
| `tests/test_marketing_assets.py` | 2026-07-02 | stale test for deleted static marketing/simulator assets | `tests/test_operating_layer.py`, `tests/test_operator_ui_browser.py` |
| `src/devflow/_legacy/` | 2026-07-02 | deleted pre-control-room runtime; current code belongs under `src/devflow/control_room/` | `AGENTS.md`, `docs/control-room-mvp.md`, `src/devflow/control_room/` |
| `src/devflow/agents/` and pure top-level legacy shims | 2026-07-02 | deleted compatibility bridges to removed `_legacy` runtime | `AGENTS.md`, `docs/control-room-mvp.md`, `src/devflow/control_room/` |
| `src/devflow/schemas/` | 2026-07-02 | deleted pre-control-room JSON schemas; current task state contract is filesystem artifacts and active control-room code | `AGENTS.md`, `docs/control-room-mvp.md`, `src/devflow/control_room/` |
| `docs/handoffs/` | 2026-07-02 | deleted stale milestone handoff archive; compact historical pointers remain in current docs and this ledger | `AGENTS.md`, `docs/control-room-mvp.md`, `docs/operator-centered-mission.md` |
| `.devflow/` tracked materialization | 2026-07-02 | untracked generated local runtime seed/state; seed authority remains in `src/devflow/control_room/seed.py` | `src/devflow/control_room/seed.py`, `README.md`, `tests/test_devflow_init_structure.py` |
| `.agent/skills/` | 2026-07-02 | deleted duplicate repo-local skill tree; `skills/` is the maintained copy and `.agent/` keeps only workflow/rule shims | `skills/`, `.agent/workflows/`, `.agent/rules/`, `.github/skills/devflow/SKILL.md` |
