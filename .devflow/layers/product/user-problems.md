# User Problems

Dev-Flow exists because parallel AI coding work becomes hard to trust when state is hidden, workers collide, logs are scattered, and failures are unclear.

Keep future entries focused on human-visible coordination problems that the control room should solve.
