# Known Gaps

- No schema validation exists yet for the seeded `.devflow/` YAML, JSON, and JSONL files.
- No command creates or repairs this structure deterministically.
- Existing legacy `.devflow/evals/`, `.devflow/plans/`, and old task artifacts have not been migrated.
- Runtime code still reflects some older product surfaces outside the frozen MVP path.
