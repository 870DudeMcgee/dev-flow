# Known Gaps

<!-- devflow:context-contract implementation-known-gaps@1 -->

Merge readiness is still human-controlled. Enabled non-shell adapters, task-fit/context routing runtime, and scheduling remain out of scope until the registry/manual/shell-alignment sequence exists.

Legacy surfaces still exist outside the frozen MVP path and must not be treated as active product authority.
