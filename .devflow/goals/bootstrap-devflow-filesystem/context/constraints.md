# Constraints

- Preserve existing repository content.
- Do not keep old-plan dumps in the active repository tree; restore only intentionally promoted active/reference material.
- Do not implement the control loop, autonomous routing, dashboard, or provider-backed adapters.
- Keep canonical state in YAML, JSON, or JSONL.
- Keep Markdown orienting and practical.
- Treat existing `.devflow/evals/`, `.devflow/plans/`, and old task artifacts as salvage candidates only; do not let them act as current authority.
