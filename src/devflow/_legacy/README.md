# Legacy Quarantine

This directory contains modules from the pre-control-room Dev-Flow product direction.

These files are historical compatibility material only. They are not active runtime authority, and new product work must not be added here.

Active implementation belongs in `src/devflow/control_room/`.

If this directory is removed or moved during manual quarantine, also remove or replace any top-level compatibility shims that import from `devflow._legacy`.
