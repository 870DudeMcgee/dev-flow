# Deferred Ideas

- Add JSON Schema or Pydantic validation for `.devflow/project`, goals, registries, and success criteria.
- Add a `devflow init` path that creates this structure deterministically.
- Add migration helpers for existing legacy `.devflow/plans/`, `.devflow/evals/`, and old task artifacts.
- Add report generation after canonical state is stable.
