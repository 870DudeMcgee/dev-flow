# Tasks

This directory is the home for bounded execution units. A canonical task owns its `task.yaml`, packet, worker report, verification result, events, questions, decisions, logs, and isolated workspace.

Existing older task-like folders in this repository are preserved as historical local artifacts unless a future migration promotes or archives them. Workers should not infer current task authority from legacy job-card files.
