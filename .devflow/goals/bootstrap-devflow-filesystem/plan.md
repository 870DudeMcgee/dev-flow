# Plan

1. Seed the smallest useful `.devflow/` context structure that matches the control-loop contracts.
2. Add a canonical bootstrap goal with machine-readable success criteria.
3. Keep active/reference context lean; archive dumps and old-plan junk stay quarantined outside the active repository tree unless a useful part is intentionally promoted.
4. Add minimal documentation pointers so future workers know where the seeded structure lives.
5. Verify formatting, parse new machine-readable files where tooling is available, and leave schema enforcement for a future slice.
