# Active Context

Active context is current guidance that workers may use for new Dev-Flow work.

Current local checkout folder: `DevFlow`. Use this folder name in active context, handoffs, and local command examples.

Only place material here when it reflects the current direction. If guidance becomes stale, rejected, deprecated, or merely historical, demote it to the matching context area instead of leaving it active.
