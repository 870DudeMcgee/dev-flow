# Locks

Locks prevent workers and humans from colliding over task, write, or branch ownership.

This directory intentionally contains no live lock files in the repository seed. Future runtime lock files such as `write.lock`, `task-<task-id>.lock`, and `branch-main.lock` are stateful artifacts created by Dev-Flow commands, not documentation placeholders.
