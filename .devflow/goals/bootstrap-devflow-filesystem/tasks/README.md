# Goal Task Links

This directory stores references from the bootstrap goal to bounded task objects. Use one `<task-id>.ref` file per task when task objects exist.

Task references are links, not task truth. Canonical task state belongs under `.devflow/tasks/<task-id>/`.
