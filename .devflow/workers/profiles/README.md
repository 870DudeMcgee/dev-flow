# Worker Profiles

Worker profiles will define permission and execution boundaries for registered workers.

Do not infer worker availability from this directory. The registry must explicitly list enabled workers before future automation can use them.
